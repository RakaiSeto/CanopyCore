name: cd

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  cd:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@main

      - name: Deploy Using ssh
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 2233
          script: |
          
            export NVM_DIR=~/.nvm
            source ~/.nvm/nvm.sh    

            PATH="/usr/local/go/bin/:$PATH"
          
            cd /github/CanopyCore
            git pull origin main
            git status
            
            cd APP/API
            go build -o canopyAPI .
            sudo mv canopyAPI /canopy/app/canopyAPI
            sudo chmod +x /canopy/app/canopyAPI
            
            cd /github/CanopyCore/RPC/RPCTest/
            go build -o testingRPC .
            sudo mv testingRPC /canopy/app/testingRPC
            sudo chmod +x /canopy/app/testingRPC

            cd /github/CanopyCore/utils/school_scraping/
            go build -o schoolScraping .
            sudo mv schoolScraping /canopy/app/schoolScraping
            sudo chmod +x /canopy/app/schoolScraping
            
            cd /github/CanopyCore/RPC/RPCGlobalData/
            go build -o RPCGlobalData .
            sudo mv RPCGlobalData /canopy/app/RPCGlobalData
            sudo chmod +x /canopy/app/RPCGlobalData
            
            sudo supervisorctl restart RPC:testingRPC
            sudo supervisorctl restart RPC:RPCGlobalData
            sudo supervisorctl restart utils:schoolScraping
            sudo supervisorctl restart API:canopyAPI
